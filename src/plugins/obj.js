const objPlungins = {
  install(app, options) {
    console.log('Loading Object Plugins....!!!', app)
    console.log('Loading Object Plugins....!!!', options)

    // app.component() 전역 컴포넌트
    // app.config.globalProperties 전역 인스턴스에 속성 추가
    // app.directive 커스텀 디렉티브
    // app.provide 리소스
  },
}

export default objPlungins
